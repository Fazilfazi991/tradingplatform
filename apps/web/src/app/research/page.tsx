import Link from "next/link";
import { researchStatus } from "@/data/demo/research-status";
import { PageHeader, SectionHeader, Tag } from "@/components/ui";
export default function Research(){return <><PageHeader title="Research lab">The real project state—completed foundations, unresolved gates, and internal experiments.</PageHeader><div className="content"><section className="section"><SectionHeader title="Data foundation and research readiness" note="Real internal history · predictive claims remain blocked"/><div className="panel status-list">{researchStatus.map(x=><div className="status-row" key={x.label}><span>{x.label}</span><Tag tone={x.status==="PASS"||x.status.includes("READY")||x.status.includes("ACTIVE")?"pass":x.status==="BLOCKED"?"blocked":x.status==="DEFERRED"?"deferred":"neutral"}>{x.status}</Tag></div>)}</div></section><section className="section"><div className="card"><h3>Prediction Engine V1 is sealed research</h3><p>Real historical features are now under chronological walk-forward evaluation. Current-universe survivorship bias, unverified corporate-action semantics, and independent reconciliation still prevent any verified or public prediction claim.</p><p style={{marginTop:14}}><Link href="/research/prediction-v1" className="inline-link">Inspect the internal V1 study →</Link></p><p style={{marginTop:10}}><Link href="/fusion" className="inline-link">Inspect Evidence Fusion V0 →</Link></p></div></section></div></>}
