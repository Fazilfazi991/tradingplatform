# Verified Edge research core

Fixture-only, leakage-safe machinery for feature/target computation, historical analogues,
chronological validation, calibration, experiment registration, and research evidence contracts.

The runtime policy currently authorizes only `ENGINEERING_FIXTURE`. It rejects formal discovery,
sealed validation, holdout, and forward modes until the external readiness gates are satisfied.
