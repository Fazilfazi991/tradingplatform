import { sectors } from "@/data/demo/sectors";
import { PageHeader, SectionHeader, Tag } from "@/components/ui";
export default function Sectors(){return <><PageHeader title="Sector intelligence">Relative strength, breadth, sentiment, flow context, and outlook across the synthetic market.</PageHeader><div className="content"><section className="section"><SectionHeader title="Sector rotation map" note="Right = stronger · up = broader participation"/><div className="panel quadrant"><div className="quad">Improving</div><div className="quad">Leading</div><div className="quad">Lagging</div><div className="quad">Weakening</div>{sectors.items.map(s=><span className="sector-dot" key={s.name as string} style={{left:`${15+(Number(s.strength)-35)*1.25}%`,top:`${88-(Number(s.breadth)-35)*1.3}%`,color:s.outlook==="Bullish"?"var(--jade)":s.outlook==="Bearish"?"var(--coral)":"var(--chalk)"}}>{s.name as string}</span>)}</div></section><section className="section"><SectionHeader title="Sector evidence"/><div className="cards">{sectors.items.map(s=><div className="card" key={s.name as string}><div style={{display:"flex",justifyContent:"space-between"}}><h3>{s.name as string}</h3><Tag tone={String(s.outlook).toLowerCase()}>{s.outlook as string}</Tag></div><p>Strength {s.strength as number}/100 · breadth {s.breadth as number}/100<br/>Sentiment {s.sentiment as string} · flow source pending</p></div>)}</div></section></div></>}
