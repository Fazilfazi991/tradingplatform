"use client";
import { useState } from "react";
import { models } from "@/data/demo/models";
import { PageHeader, SectionHeader, Tag } from "@/components/ui";
export default function Models(){const [open,setOpen]=useState<string|null>(null);return <><PageHeader title="Seven evidence engines">Each specialist examines a different evidence class. This public registry describes synthetic demonstration behavior, not live predictive capability.</PageHeader><div className="content"><section className="section"><SectionHeader title="Public demonstration registry" note="Select an engine for details"/><div className="cards">{models.map(m=><button className="card" style={{textAlign:"left"}} key={m.name} onClick={()=>setOpen(open===m.name?null:m.name)} aria-expanded={open===m.name}><Tag tone={m.status==="Unavailable"?"blocked":"demo"}>{m.status}</Tag><h3 style={{marginTop:20}}>{m.name}</h3><p>{m.purpose}</p>{open===m.name&&<div style={{marginTop:18,paddingTop:14,borderTop:"1px solid #293934"}}><p><b>Public inputs:</b> {m.sources}<br/><b>Public state:</b> {m.status}<br/><b>Demo version:</b> {m.version}</p></div>}</button>)}</div></section></div></>}
