import Link from "next/link";
import { researchStatus } from "@/data/demo/research-status";
import { PageHeader, SectionHeader, Tag } from "@/components/ui";
export default function Research(){return <><PageHeader title="Research lab">The real project state—completed foundations, unresolved gates, and internal experiments.</PageHeader><div className="content"><section className="section"><SectionHeader title="Data foundation and research readiness" note="Fixed internal history · current operations shown separately"/><div className="panel status-list">{researchStatus.map(x=><div className="status-row" key={x.label}><span>{x.label}</span><Tag tone={x.status==="PASS"||x.status.includes("READY")||x.status.includes("ACTIVE")?"pass":x.status==="BLOCKED"?"blocked":x.status==="DEFERRED"||x.status.includes("UNAVAILABLE")||x.status.includes("STOPPED")?"deferred":"neutral"}>{x.status}</Tag></div>)}</div></section><section className="section"><div className="card"><h3>Prediction Engine V1 is sealed research</h3><p>Chronological walk-forward evaluation and the one-time sealed holdout are complete. The result remains candidate evidence only. Current-universe survivorship bias, unverified corporate-action semantics, independent reconciliation, and absent prospective outcomes prevent any verified or public prediction claim.</p><p style={{marginTop:14}}><Link href="/research/prediction-v1" className="inline-link">Inspect the internal V1 study →</Link></p><p style={{marginTop:10}}><Link href="/fusion" className="inline-link">Inspect the synthetic Evidence Fusion demo →</Link></p></div></section></div></>}
