# Evidence Fusion Engine V0

Status: **V0 ACTIVE / ENGINEERING FIXTURE**. Predictive validation: **NOT STARTED**.

```text
seven causal specialist snapshots
  -> unified evidence adapters/contracts
  -> quality, freshness and eligibility
  -> provenance dependency graph
  -> contributions and contradictions
  -> agreement and uncertainty
  -> abstention policy
  -> immutable FusionSnapshot + explanation
```

All inputs share scope, horizon and logical cutoff. Missing or failed engines remain insufficient rather
than neutral. The engine uses categorical quality precedence, never permanent predictive weights. Replay
with the same inputs and `fusion-v0` policy produces the same semantic state and payload hash.

Pre-market fusion runs after upstream snapshots at 09:00 IST; EOD fusion follows positioning at 18:40.
Missing upstream snapshots produce abstention rather than silent stale substitution.
